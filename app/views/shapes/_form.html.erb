<%= simple_form_for(@shape) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :title_eng %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
